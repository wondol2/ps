#include <bits/stdc++.h>
using namespace std;

#ifndef ONLINE_JUDGE
#include "debug.h"
#else
#define debug(x)
#endif


void solve(){
    int H, W, K; cin >> H >> W >> K;
    int A[200001], B[200001];
    for(int i=0; i<H; ++i) cin >> A[i];
    for(int i=0; i<W; ++i) cin >> B[i];
    
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int T=1;
    // cin >> T;
    while(T--){
        solve();
    }

    return 0;
}
